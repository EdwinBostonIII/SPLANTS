# SPLANTS Web App Environment Configuration
# Copy this file to .env and adjust values for your environment.

# Base URL used for API requests from the web UI.
# In local development the backend is proxied at /api via Vite or nginx.
VITE_API_BASE_URL=/api

# Optional: default API key to send with requests.
# Leave blank to require users to enter their key manually.
VITE_API_KEY=
